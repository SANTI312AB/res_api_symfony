nelmio_api_doc:
    documentation:
        info:
            title: My API
            description: This is an awesome API!
            version: 1.0.0
        components:
            securitySchemes:
                Bearer:
                    type: http
                    scheme: bearer
                    bearerFormat: JWT
        security:
            - Bearer: []
    
    areas:
        path_patterns:
            - '^/((?!(?:_error|_wdt|_profiler)).)*$'
